## 1. Mission

l00prite is a vendor-neutral protocol that gives AI coding agents durable, file-based
project memory and a deterministic execution protocol. It scaffolds a project blueprint, a
`.l00prite/` memory folder (including canonical loop prompts), a vendor-neutral `AGENTS.md`,
per-vendor adapters, and Claude/Codex prompt mirrors into a target repo so any agent —
Claude, Codex, GPT, Gemini, Copilot, Cursor, Windsurf, Aider, or a future CLI agent — can
pick up where another left off, and can run autonomous Execution Mode loops behind an
explicit pre-flight gate, using files in the repo instead of vendor-specific session state.

Target user: developers who want AI agents to build software across multiple sessions and
multiple agent vendors without losing context, without silent unreviewed execution, and
without every handoff depending on one tool's proprietary memory.

This CLAUDE.md describes l00prite's own repo — the protocol and its tooling — not a project
generated by l00prite. It reflects what is actually built today, not a design still in
progress.

## 2. Architecture

l00prite has two operating modes — **Planning Mode** (scaffold and stop) and **Execution
Mode** (autonomous run behind a confirmed pre-flight) — implemented across six protocol
layers, all plain Markdown and JSON:

1. **Scaffold layer (Planning Mode)** — `.claude/commands/build-loop.md` (Claude Code slash
   command) and `.codex/prompts/build-loop.md` (Codex/CLI equivalent) ask clarifying
   questions, pick a complexity tier, and write a target project's `CLAUDE.md` (with a
   fixed protocol section), `AGENTS.md`, `.l00prite/` memory folder, prompt mirrors, vendor
   adapters, and a tiered skeleton — then stop. Planning Mode never executes the generated
   project and always ships Execution Mode disarmed.
2. **Memory layer** — `templates/l00prite/` defines the `.l00prite/` folder shape copied
   into every scaffolded project: `blueprint.md`, `ledger.md`, `memory.md`, `constraints.md`,
   `failures.md`, `todos.md`, `heartbeat.json` (schema v2, with the `execution` block),
   `state.json` (schema v2, with execution-run fields), `lock.json`, `LOCKING.md`,
   `prompts/` (the canonical loop prompts — see layer 5), `events/`, `reviews/`,
   `sessions/`. `examples/vendor-neutral-output/` is a filled reference copy of the
   scaffold output (adapters included; the `.claude/`/`.codex/` prompt mirrors and the
   tier skeleton are omitted — its README explains why).
3. **Event layer** — pending interrupts (PR review comments, failed CI runs) are modeled as
   first-class JSON objects moved through `pending/ → processing/ → completed/` and handled
   via the event-loop/respond-to-review prompts
   (`Classify → Plan → Execute → Verify → Persist → Respond`), capped at one event per loop
   by default. Event content is always untrusted data.
4. **Handoff layer** — resume-loop, heartbeat, respond-to-review, and handoff-summary
   prompts let an agent resume one supervised step, decide whether to continue, respond to
   reviews, and hand off, all through shared `.l00prite/` files.
5. **Universal prompt + vendor layer** — the six loop prompts live canonically in
   `templates/l00prite/prompts/` and are mirrored byte-identically into `.claude/prompts/`,
   `.codex/prompts/`, `templates/claude/prompts/`, `templates/codex/prompts/`, this repo's
   own `.l00prite/prompts/`, and the example output — enforced by the validator, so parity
   can't drift. `templates/AGENTS.md.template` generates the vendor-neutral operating guide
   (read natively by the AGENTS.md ecosystem), `templates/adapters/` ships self-sufficient
   adapters for Gemini CLI, Qwen Code, GitHub Copilot, Cursor, Windsurf, and Aider, and
   `templates/vendors.json` is the machine-readable manifest. The adapters are dogfooded at
   this repo's own root.
6. **Execution layer (Execution Mode)** — `execute-loop.md` (canonical prompt, mirrored
   everywhere, plus the `/execute-loop` slash command) defines the autonomous run: a
   mandatory pre-flight gate (lock check first, stale-run recovery, schema migration,
   pre-flight display, explicit in-session human confirmation that persisted flags never
   satisfy), an iteration protocol (one verified, persisted unit per iteration), nine run
   boundaries (`definition_of_done_met`, `iteration_limit_reached`, `human_review_gate`,
   `destructive_operation_required`, `ambiguous_requirements`, `unfixable_failing_tests`,
   `missing_secrets_or_credentials`, `lock_lease_conflict` — which writes nothing to memory
   another agent holds — and `stop_signal`), resumable exits, per-action permission for
   push/merge/deploy/credentials, and a self-modification guard (the loop can never raise
   its own limits). `build-loop --execute` offers the handoff after scaffolding but never
   skips the gate.

Cutting across all layers: `.l00prite/lock.json` plus `LOCKING.md` document a cooperative
lock/lease convention (check before writing, acquire if unlocked/released/expired, respect
an active unexpired lock owned by someone else, reclaim-and-log a stale one, release before
stopping) so agents working close together in time reduce — though do not eliminate — the
risk of racing writes to shared memory files. `prompts/` files are protocol files: never
agent-modified during a loop.

No backend, no hosted service, no external dependencies. Everything is prompt files, JSON
schemas, and `scripts/validate-l00prite.js`, a Node script with no dependencies of its own.
Execution Mode is a protocol the agent follows — its invariants are validator-enforced
prompt text, not a runtime harness that mechanically forces compliance (that harness is
roadmap).

## 3. Requirements

- [x] Scaffold a project blueprint (`CLAUDE.md`) from a clarifying-questions conversation.
- [x] Scaffold a `.l00prite/` memory folder into the target repo, never overwriting
      existing files silently.
- [x] Provide Claude Code (`.claude/commands/`, `.claude/prompts/`) and Codex
      (`.codex/prompts/`) workflows with feature parity between the two.
- [x] Support resumable loops: a new session can read `.l00prite/` and continue the next
      smallest useful step.
- [x] Support heartbeat control (`heartbeat.json`) to decide whether a loop should continue,
      pause, or stop.
- [x] Model PR review comments and CI failures as first-class, trackable events with a
      documented lifecycle and ID format.
- [x] Provide a lock/lease convention so agents working close together in time do not
      silently corrupt shared memory files.
- [x] Provide a validator (`scripts/validate-l00prite.js`) that checks structural and
      protocol invariants across prompts, templates, and examples — including byte-identical
      prompt mirrors and vendor adapter integrity.
- [x] Universal agent layer: canonical prompts inside `.l00prite/prompts/`, a generated
      `AGENTS.md`, and self-sufficient adapters so every major agent (Codex, Cursor,
      Copilot, Windsurf, Zed, Gemini CLI, Qwen Code, Aider, opencode, and the rest of the
      AGENTS.md ecosystem) discovers the protocol through its own context file.
      *(implemented on this branch — in review)*
- [x] Opt-in Execution Mode: `execute-loop` prompts everywhere, a `--execute` handoff on
      `build-loop`, a pre-flight display + explicit in-session confirmation gate, nine run
      boundaries, resumable exits, and schema v2 execution fields in
      `heartbeat.json`/`state.json`. *(implemented on this branch — in review)*

## 4. Definition of Done

**v1 (shipped):**

- [x] `node scripts/validate-l00prite.js` passes with zero FAIL lines.
- [x] Claude and Codex prompt sets have feature parity.
- [x] README accurately documents what l00prite does and does not do.
- [x] The repo has been dogfooded against its own protocol at least once.

**v1.1 — universal agent layer + Execution Mode (this branch):**

- [x] `node scripts/validate-l00prite.js` passes with zero FAIL lines, including the new
      byte-parity, adapter, execution-schema, and execute-loop invariant checks.
- [x] The six loop prompts are byte-identical across all seven locations, validator-enforced.
- [x] Every scaffolded project carries `AGENTS.md`, the vendor adapters, and
      `.l00prite/prompts/`; the example output mirrors the real scaffold.
- [x] Execution Mode ships disarmed everywhere, enters only via the confirmed pre-flight,
      and can never raise its own limits.
- [ ] The maintainer has reviewed and merged this branch to `main` — including the gated
      changes to `.claude/commands/build-loop.md` and `scripts/validate-l00prite.js`.

## 5. Agent Operating Loop

- **Generator role** — writes or modifies one file per iteration, verifying each change
  before moving to the next. Does not invent requirements beyond what is stated in Section 3.
- **Evaluator role** — after each file is written or modified: runs
  `node scripts/validate-l00prite.js` when a validated file changed, checks JSON files parse,
  and rejects any change that silently alters Planning Mode's non-executing default or lets
  Execution Mode start without its confirmed pre-flight gate.
- **Loop description** — generate one file → evaluator checks it → if it passes, update
  `.l00prite/ledger.md` and `.l00prite/todos.md` → move to the next file. If the evaluator
  rejects a change, fix it in the same iteration before moving on. Do not batch multiple
  unrelated files into one step.

## 6. Heartbeat Rules

- **Human review gates** — mandatory human review before: (1) any change to
  `.claude/commands/build-loop.md` (core scaffold command), (2) any change to
  `scripts/validate-l00prite.js` (validator logic), (3) declaring a release's Definition of
  Done met.
- **Branch policy** — feature work on a feature branch; no direct commits to `main`; each
  file change is its own commit describing what was written and what was verified; open a
  PR for maintainer review before merging.

## 7. Run Ledger

| Session | Date | Built | Tested | Status |
|---------|------|-------|--------|--------|
| Scaffold bootstrap | 2026-06-30 | `/build-loop` slash command, Loop Engineering scaffolding | Manual review | Merged |
| Dogfood pass | 2026-06-30 | Ran `/build-loop` on this repo, fixed 4 bugs found, updated Run Ledger | Manual review | Merged |
| Codex parity | 2026-07-01 | `.codex/prompts/` equivalents of the Claude workflow | `node scripts/validate-l00prite.js` | Merged |
| README repositioning | 2026-07-01 | Repositioned README as vendor-neutral loop memory protocol | Manual review | Merged |
| Execution-mode design note | 2026-07-01 | Drafted execution-mode design directly into `CLAUDE.md` (no code) | None — design only | Superseded; design preserved in `.l00prite/todos.md` history and realized in the v1.1 pass below |
| Protocol hardening | 2026-07-01 | Lock/lease convention (`lock.json`, `LOCKING.md`), untrusted-content warnings on event/review prompts, Claude/Codex prompt parity, event ID format fix, ledger verification-evidence fields | `node scripts/validate-l00prite.js` | Merged |
| Branding | 2026-07-01 | Replaced ASCII banner with block letter art in README | Manual review | Merged |
| Pre-release polish | 2026-07-01 | Corrected `CLAUDE.md` to describe actual repo state; updated `HANDOFF.md`, `README.md` roadmap, `.l00prite/` (new, scaffolded for this repo); added `RELEASE.md` | `node scripts/validate-l00prite.js` | Merged |
| Universal agent layer + Execution Mode | 2026-07-02 | Canonical prompts in `templates/l00prite/prompts/` with byte-identical mirrors; `execute-loop` (pre-flight gate, nine run boundaries, self-modification guard); `AGENTS.md.template` + `CLAUDE.md.template` protocol section; vendor adapters + `vendors.json`; schema v2 (`execution` block, execution-run state); build-loop reframed as Planning Mode with `--execute` handoff; validator byte-parity/adapter/execution checks; docs reframed around two operating modes | `node scripts/validate-l00prite.js` (zero FAIL), byte-parity `cmp` across all mirrors, adversarial multi-agent design/code review | In review |
| Loop-maturity gap pass (from loop-engineering analysis) | 2026-07-04 | Read-only `scripts/l00prite-doctor.js` health check for scaffolded projects; `docs/` loop-wisdom catalogs (failure-modes/anti-patterns/concepts, S1/S2/S3); seeded inherited failure catalog in `failures.md`; machine-readable Autonomous-Edit Denylist in `constraints.md` enforced via the existing `destructive_operation_required` boundary; additive no-progress telemetry (`iterations_since_progress`/`last_progress_iteration`/`no_progress_threshold`) + execute-loop maintenance + doctor stall check. Fable 5 advised the scope; **zero** edits to the two review-gated files; deferred gated work queued as one v1.2 batch | `node scripts/validate-l00prite.js` (zero FAIL, 519 PASS), `node scripts/l00prite-doctor.js .` (HEALTHY), byte-parity `cmp` across all 7 execute-loop copies, doctor negative test | In review |
| CLI-OS onboarding pass | 2026-07-04 | Scheme-aware form-control theming in `cli-os` dashboard + setup wizard (fixes black-on-black inputs in light-scheme modals); demo/mock removed from all user-facing flows (wizard, dashboard add-provider, docker seed, `init` hints, quickstarts) while kept as an internal test adapter; authenticated `POST /v1/repos` + `/v1/repos/remove` with a dashboard Register-repo modal; dashboard Playground (model/repo pickers + custom-model entry over the existing authenticated `/v1/chat/completions`); root `GETTING_STARTED.md` + README quickstart callout; installer next-steps rewritten browser-first. Zero edits to the two review-gated files | `go test ./...` (all pass, incl. new repo-mgmt tests), `node scripts/validate-l00prite.js` (zero FAIL, 519 PASS), Playwright end-to-end against the real binary (18/18 checks: input contrast ~17:1 in both color schemes, repo registered via the UI, playground round-trip rendered) | In review |
| L00prite OS core (OS-APK) | 2026-07-05 | `cli-os/docs/os-architecture.md` (v2 design); `internal/engine/` run engine mechanically embodying `execute-loop.md` (pre-flight steps 1-5 in code, Start = the explicit in-session confirmation via `confirm:"EXECUTE"`, one-unit iterations, all nine run boundaries as code, repo-jailed tools with protocol-file hard-deny + Autonomous-Edit Denylist + command-allowlist gates, per-action approvals fail-closed on timeout, dual persistence engine-SQLite + target-repo `.l00prite/`, crash recovery) — realizes the "runtime harness" roadmap item; role-aware routing (`roleRanks`, profile `rankMap`/`providers`, built-in plan/code/review/summarize profiles); `EngineCaller` seam (every autonomous call routed/PEP-reserved/metered/ledgered; the engine names only `auto:<profile>`); `/v1/runs*` API + `/v1/repos/clone`; cross-platform packaging (`scripts/dist.sh` 5-target static matrix + SHA256SUMS, `install.ps1`, `l00prite version`). Fable 5 designed and wrote the engine core; Opus writers built peripheral units to spec. Dashboard Runs view queued next. Zero edits to the two review-gated files | `go test ./...` (all pass incl. engine unit suites + 4 end-to-end run tests: done-with-real-writes/disarmed-exit/released-lock, denylist gate fail-close to `destructive_operation_required`, Start refused without fresh pre-flight+confirm, crash reconcile), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), `node scripts/l00prite-doctor.js .` (HEALTHY), `bash scripts/dist.sh vtest` (5 artifacts + SHA256SUMS) | In review |
| Android APK (L00prite OS on-device) | 2026-07-05 | `cli-os/docs/android-architecture.md` (feasibility decision: bundle the unmodified gateway as an android/arm64 PIE binary exec'd from the APK's native-lib dir under a thin no-AndroidX Java wrapper + WebView — gomobile/Termux/native-rewrite rejected; G1–G11 platform gap analysis; Phase 0–3 roadmap); Go Android enablement (`LOOPRITE_DNS` resolver hook, shell-path resolution, `internal/gitx` seam: exec-git verbatim default + pure-Go go-git fallback for git-less hosts, `LOOPRITE_SETUP_SECRET` first-run gate closing the loopback setup race, secret env scrubbing); `manifests/venice.json` (dedicated Venice AI path — 15 models, pricing first-party-verified via Venice's docs mirror) + `manifests/gemini.json`; `architect`/`writer`/`reviewer`/`advisor` role profiles + seeded roleRanks incl. engine `plan`/`code`/`review` (Fable 5 architect 98, Sonnet 5 writer 97; `writer`+`code` flipped balanced→quality so the role policy actually routes — flagged for maintainer); `android/` Java wrapper (foreground-service exec, Android-Keystore-wrapped master key via env, Mozilla-only CA bundle, cleartext-to-loopback-only WebView); `cli-os/scripts/build-apk.sh` (hermetic no-Google toolchain) + `.github/workflows/android-apk.yml` (real-SDK CI — a Denylist path, shipped via this reviewed PR). Fable 5 architected and reviewed; Sonnet 5 writers built the units. Zero edits to the two review-gated files | `go test ./...` (all pass incl. gitx both-implementations suite, setup-secret gate, role-routing tests), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), e2e gateway smoke 15/15 (wizard latch, venice `/v1/models`, mock chat round-trip, dry-run `auto:writer`→sonnet via roleRanks.writer), `apksigner verify` v2+v3 true on the final 15MB APK (arm64-v8a PIE payload, interpreter `/system/bin/linker64`), `node scripts/l00prite-doctor.js .` HEALTHY post lock-release | In review |
| Android APK Phase 1+2 (Runs UI, on-device autonomy) | 2026-07-06 | **Phase 1** — dashboard Runs view (`cli-os/public/dashboard.html`): full create/pre-flight/live/exit lifecycle over the existing `/v1/runs*` API, exact-match "EXECUTE" Start gate, esc()'d 2s-polled event feed, approvals inbox, Resume-always-through-fresh-preflight, repo clone-from-URL, phone-first hamburger nav; wizard copy (`setup.html`) de-CLI'd. **Phase 2** — `git_command` read-only subset (status/diff/log/show, exact-match argument shapes only) mapped onto go-git (`internal/gitx` gains `Log`/`Show`, verified `Commit.Patch` direction so an added file renders as a genuine addition); ledger JSONL rotation (`internal/ledger`, mutex-guarded against concurrent HTTP-request callers, 5 MiB default + one backup generation); native Android SAF repo-import (`MainActivity.java`, `DocumentsContract` since `DocumentFile` is AndroidX-only and confirmed absent from the platform jar, canonical-path containment checked at every recursive level). ssh clones, the Termux/remote-verifier bridge, and SAF export explicitly scoped OUT and deferred with recorded rationale (no on-device key-provisioning UI; needs its own design pass; lower urgency than import, respectively). Sonnet 5 wrote both phases to architect-specified contracts; Opus 4.8 adversarially reviewed (zero blocking findings across both phases) and ran live verification (Phase 1: real Playwright e2e, 10/10; Phase 2: git_command subset live-driven with git stripped from PATH, ledger stress-tested at 2500 concurrent appends). Fable 5 architected, scoped, and independently re-verified safety-critical code before each commit. Zero edits to the two review-gated files | Phase 1: `go test ./...`, validator 0 FAIL, Playwright e2e 10/10 zero console errors, APK rebuilt+resigned. Phase 2: `go test -race ./...` zero races, `CGO_ENABLED=0 GOOS=android GOARCH=arm64 go build` clean, validator 0 FAIL, APK rebuilt+resigned, `apksigner verify` v2+v3 true, `strings classes.dex` confirms the SAF code actually compiled in (device-level SAF behavior explicitly left unverified — no emulator ABI in this container) | In review |
| Marketing site + beta APK download | 2026-07-06 | Root `index.html` — self-contained, GitHub-Pages-ready static landing page (zero external requests: system fonts, inline SVG): brand-matched circuit design from `assets/brand-image.png`, what-LOOPRITE-does cards on the repo-as-OS metaphor, Planning/Execution mode panels with an animated execute-loop pre-flight terminal, Android section (phone mockup, sideload steps, SHA-256 copy button), beta warning strip, l00prite source attribution; `.nojekyll`; built `l00prite-os-0.1.0-beta.apk` with the hermetic `build-apk.sh` and shipped it in-repo at `downloads/` (+ `SHA256SUMS`) so the download link works before any GitHub release exists; `.github/workflows/deploy-vercel.yml` — Vercel production deploy (repo-identity double fence: job-level `if` + hard-fail step so only `jackofall1232/LOOPRITE` — never the public source-only `l00prite` upstream or other forks — can publish; stages only `index.html`/`.nojekyll`/`assets/`/`downloads/` with the same hard-fail asset guard; ~100 MiB per-file size check with 80% early warning; `vercel deploy --prod` via `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` repo secrets; push-to-`main` + manual dispatch; supersedes the interim GitHub Pages workflow authored earlier on this branch). Zero edits to the two review-gated files | `bash cli-os/scripts/build-apk.sh 0.1.0-beta` (`apksigner verify` v2 true, all badging checks pass, sha256 ef9f8a02…), Playwright desktop 1440px + mobile 390px (zero console errors, zero failed requests, all APK links 200 at 15,291,037 bytes, SHA copy button works, no mobile overflow), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), workflow YAML parse + local dry-run of the Pages artifact assembly (17M, all site files present) | In review |
| Repo-scope footgun fix (dashboard debugging pass) | 2026-07-10 | Root-caused the maintainer-reported "repo not registered" prompt failure (not a GitHub-permissions issue — the setup wizard minted tokens scoped to any free-text string, e.g. `owner/repo`, that can never match a registered repo id): `/v1/setup/token` now rejects never-registrable scopes (400 `invalid_repo_scope`) and warns on well-formed-but-unregistered ones (wizard copy reframed "advanced, leave empty" + client-side validation + warning surfaced on the done screen); chat/runs "not registered" errors now distinguish token-scope vs header, name the exact repair, carry `repo_not_found`, and state GitHub permissions are not involved; dashboard Playground shows a persistent warning with a one-click prefilled Register action when the token's scope is unregistered (or the re-mint command when the scope can never be registered); register modal + auth-shaped clone failures explain public-repos-need-no-permissions with an SSH-key docs link for private ones. Zero edits to the two review-gated files | `go test ./...` (all pass incl. new e2e `TestRepoScopeFootgun` regression walking the full user story: bad scope refused → warned mint → actionable 404 → registering the exact id heals the token), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), live before/after reproduction against the rebuilt binary | In review |
| Android launcher icon + sideload guidance | 2026-07-10 | Adaptive infinity (♾) launcher icon — neon figure-eight with layered-stroke glow, white-hot crossover node, circuit-trace corners, Android 13+ monochrome themed layer — as pure vector drawables the hermetic aapt-v1 pipeline can compile (`android/res/drawable/` + `mipmap-anydpi-v26/`, manifest `android:icon`/`roundIcon`; the APK previously shipped with no icon at all); site `index.html` install steps grown 4→5 with a leading "Prep your phone" step (per-app Install-unknown-apps path, Samsung variant, flip-back-off tip) plus an amber "Your phone will warn you — that's expected" explainer covering the exact Chrome → unknown-apps → Play Protect warning sequence, why it happens (developer-signed, outside Play = unrecognized not malicious), SHA-256/build-from-source verification, and revoke-permission/uninstall-before-upgrade hygiene; rebuilt `downloads/` beta APK + `SHA256SUMS` + page SHA so the shipped download carries the icon. Zero edits to the two review-gated files | `bash cli-os/scripts/build-apk.sh 0.1.0-beta` (badging shows `application-icon: res/mipmap-anydpi-v26/ic_launcher.xml`, all script asserts pass, sha256 32e0feb7…), `apksigner verify` v2+v3 true, Chromium render of an exact SVG replica under circle/squircle/square/themed-mono masks, Playwright #android at 1440px+390px (zero console errors, zero overflow), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL) | In review |
| Control-plane diagnosis + fix (Bugs 1 & 2) | 2026-07-11 | Diagnosis (`RUN_LEDGER.md`, repo root): Bug 1 traced to no file-access tool on the ordinary chat path plus `.l00prite/` never auto-scaffolded at link time; Bug 2's original hypothesis (untracked scaffold files) refuted by adversarial verification and replaced with the real trigger (`StartRun`'s lock-acquire rewriting an already-committed `lock.json` right before a gogit checkout with no path-exemption). Implementation, same session: Bug 2 — `AutoCheckpoint` (`internal/engine/tools.go`) wired into `StartRun` between `AcquireLock` and `EnsureRunBranch`, `checkGitReady`'s dirty-tree Blocker split into a Note, `humanizeStartError` translation layer (`gateway/runs.go`). Bug 1 — new self-contained read-only chat toolbox (`gateway/chattools.go`, explicitly not `engine.Toolbox`) plus `RunChatTools` (`gateway/chatloop.go`, mirrors `RunBridge`'s bounded-loop shape), auto-scaffold on repo register/clone. A second adversarial review (3 reviewers + skeptical re-verification + senior synthesis) then found and fixed 9 confirmed issues in the first pass: a symlink-alias and two other secret-deny-list bypasses, AutoCheckpoint committing Denylisted/secret files with no gate (most severe — closed with a new `ErrCheckpointRefused` two-layer check), cross-round cost/usage silently dropped, raw git error text still leaking via pre-flight Blockers (an explicitly-requested §2.2 item missed in the first pass), a scaffold-identity bug, a client tool-name collision, and a misleading test with a real coverage gap (closed with an end-to-end gogit HTTP test). Fable 5 diagnosed, verified, and synthesized both review rounds; Sonnet 5 implemented and fixed. Base-branch lineage (pre-existing, needs a design pass) and `list_dir`'s missing symlink filter (cosmetic) deliberately deferred, not silently dropped. Zero edits to the two review-gated files; no PR opened (not requested) | `go test -race ./...` (all packages green, incl. ~20 new regression tests — several manually confirmed to fail against the pre-fix code before landing, incl. one reproducing the exact original raw go-git error text), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL) | Merged (PR #6) |
| PR #6 review fixups (Gemini + Codex automated review) | 2026-07-11 | A pull request (PR #6) was opened from the diagnosis+fix branch above; two automated reviewers left findings, both fixed following the same empirical-reproduction discipline (`RUN_LEDGER.md` §8 has the full record). Gemini round (6 findings): a `limit_lines` integer-overflow panic in the chat toolbox's `readFile` (the real danger zone is `math.MaxInt64-2047`, not `math.MaxInt64`, due to float64 precision near 2^63), a path-containment check tightened to `filepath.Rel`-based reasoning, platform-independent secret-path matching, byte-index line truncation, plus a flaky-CI fix (a background engine goroutine racing `t.TempDir()` cleanup after Start). Codex round (9 findings, the most severe first): a ledger-append race that reintroduced Bug 2's exact symptom with `ledger.md` as the trigger instead of `lock.json` (`StartRun`'s checkpoint ledger entry now written after `EnsureRunBranch`'s checkout, not before); an untracked-directory collapse in real git's default `status --porcelain` output that hid secrets from `AutoCheckpoint`'s gate under the exec backend (`--untracked-files=all` added); repo registration ignoring an active foreign lock before scaffolding; an unbounded `read_file` load before truncation; an unbounded `search_files` file-walk; a round-cap loop exit that could leave dangling `tool_calls` in the response sent to the client; a stale `tool_choice` referencing a tool `stripChatTools` had just removed; and a failed `CommitUnit` commit leaving `git add -A`'s staging in place (best-effort `git reset` via the exec backend's `Raw()`, with the gogit-backend gap documented rather than silently left). Every fix has a dedicated regression test, each manually confirmed to fail against the pre-fix code first. Zero edits to the two review-gated files across both rounds; all six commits pushed to the existing PR #6 branch (no new PR opened) | `go build ./...`, `go vet ./...`, `go test -race ./...` (all packages green after every commit), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL) | Merged (PR #6, into ANDROID at 56090e1) |
| ANDROID → main conflict-resolution merge (PR #7) + review fixups | 2026-07-11 | Opened PR #7 (`ANDROID` → `main`; the two branches shared only the repo's very first commit as a common ancestor). Resolved 10 conflicting files by judgment per file, not a blanket "ours"/"theirs": append-only files (`ledger.md`, `CLAUDE.md`'s Run Ledger) got both sides' entries concatenated in chronological order; functions PR #6 had already fixed on `ANDROID` (`checkGitReady`, `CommitUnit`, `StatusPorcelain`) took `ANDROID`'s side wholesale over `main`'s stale pre-fix copies; `dashboard.html` and `repos_clone.go` got both sides' independent additive changes combined (verified via call-site tracing, e.g. `main`'s `play-scope-warn` listener needed `ANDROID`'s `addRepo` signature to gain the `prefillId` param it never had). A second Codex review on PR #7 then found 5 more confirmed issues, each verified against the unfixed code and fixed: Android's on-device master key persisted with fire-and-forget `apply()` instead of synchronous `commit()` (session-fatal data loss on a mistimed kill); the gateway monitor-thread restart guard checked only for non-nil instead of `isAlive()`; repo registration scaffolded `.l00prite/` after only reading lock state, never acquiring it; `StartRun`'s lease-acquire write landed in the pre-run checkpoint commit on the user's own SOURCE branch (not the run branch) because it ran before `EnsureRunBranch`, not after — the same structural class of bug PR #6's ledger-ordering fix addressed, just for `lock.json`'s active-lease write instead of a ledger append; and a repo-scoped token could list every other repo's runs via `/v1/runs/list` since that handler filtered only by project, never by the token's repo scope. Toolchain (`aapt`/`zipalign`/`apksigner`/`dalvik-exchange`/`android-framework-res`) installed via apt in-session to verify the Java edits actually build. Zero edits to the two review-gated files | `go build ./...`, `go vet ./...`, `go test -race ./...` (all packages green after every commit), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL); each of the 5 PR #7 findings verified by temporarily reverting the fix, confirming a new/existing test reproduces the exact reported symptom, then restoring it | In review (PR #7) |
| v0.2.0-beta release: version bump, changelog, site refresh | 2026-07-11 | First formal `CHANGELOG.md` (root, new) documenting v0.1.0-beta and v0.2.0-beta — the latter summarizing everything shipped since the initial beta APK (Bug 1/2 fixes, both PR #6 review rounds, the PR #7 merge + its 5 review fixups). Rebuilt the Android APK as `l00prite-os-0.2.0-beta.apk` via the hermetic `build-apk.sh` pipeline (toolchain installed via apt this session); `index.html` updated at all 6 version/filename references plus the SHA-256 checksum shown on the download card; `downloads/SHA256SUMS` regenerated; old `l00prite-os-0.1.0-beta.apk` removed. `README.md` gained a short pointer to the site download and the new changelog (the root README itself documents the protocol, not the APK, so it needed no version-specific content). Zero edits to the two review-gated files | `apksigner verify` v2+v3 true on the rebuilt APK (byte-identical copy in `downloads/` vs. the build output), `aapt dump badging` confirms `versionName='0.2.0-beta'`, inline site JS parse check, `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL) | In review |
| Budget/overage controls, chat-triggered draft runs, model lock, in-app Help (v0.3.0-beta) | 2026-07-11 | Four maintainer-requested Android-app-facing features (the app's whole UI is this dashboard.html, WebView-served from the embedded gateway binary), built by a Fable-architected / Sonnet-authored / Opus-reviewed pass: (1) per-project daily budget with a bold "Set budget" modal offering stop-at-limit / 100%-overage / no-stoppage modes, via new `caps.overage_pct`/`unlimited` columns and an effective-ceiling calculation in `policy/pep.go`, plus `GET`/`POST /v1/budget`; (2) a `propose_run` chat tool so a plain-text Playground prompt ("…and execute it") can draft a reviewable Run through the exact same `CreateRun`+`BuildPreflight` path as the dashboard's "New run" button — refactored into a shared `createDraftRun` helper so a chat-drafted run can structurally never reach `StartRun`, preserving the EXECUTE-confirmation invariant; (3) a Playground model-lock toggle persisting the chosen model across reloads; (4) a new in-app Help section (getting started, Playground incl. run-drafting, Runs' pre-flight/EXECUTE gate, the three budget modes) with six screenshots captured live against a real mock-provider-backed gateway (incl. a genuinely Started-and-suspended run for an authentic live-feed/approval capture) — one adversarial look at the images against their captions found and fixed two defects (a poorly-framed shot recaptured as a tight element crop; a mismatched alt-text corrected) before shipping. One adversarial-review finding fixed: `l00prite cap set`/`cap list` (CLI) silently preserved a stale `unlimited`/`overage_pct` flag instead of resetting it, defeating a re-imposed cap. Rebuilt the Android APK as `l00prite-os-0.3.0-beta.apk` so the shipped binary actually embeds the new dashboard.html/screenshots; `CHANGELOG.md`, `index.html` (version/filename/size/SHA-256), and `downloads/` updated to match. Zero edits to the two review-gated files | `go build/vet/test -race` (all packages, cli-os, incl. new focused tests for overage/unlimited enforcement and the `propose_run` safety gating), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), adversarial code review confirming the EXECUTE-confirmation and cost-cap invariants both hold, `apksigner verify` v2+v3 true on the rebuilt APK, `sha256sum -c` against `downloads/SHA256SUMS`, agent-viewed verification of all 6 screenshots against their captions (2 defects found and fixed) | In review |
| xAI Grok + Gemini providers, Venice made selectable (provider-presets dropdown) | 2026-07-12 | Workflow-orchestrated pass (Sonnet research → Fable design → Opus implementation → Fable adversarial verify) adding first-class "Add provider" presets. Research: xAI's own docs domains were egress-blocked; fell back to xAI's official GitHub SDK/cookbook and confirmed **Grok 4.5** is real (announced 2026-07-08) and that the retired `grok-code-fast-1` has been superseded by **`grok-build-0.1`** as xAI's current coding model. Google's pricing pages WERE reachable this session, upgrading `gemini.json` from null/unconfirmed to first-party pricing and confirming **Gemini 3.5** (`gemini-3.5-flash`) is real/GA. Venice re-check: zero drift. Design decoupled "which provider preset was picked" from "which wire adapter kind is POSTed" (the old 2-option dropdown conflated them) via new unauthenticated `GET /v1/providers/catalog` (`adapters.Presets()`, same non-secret class as `/v1/setup/status`) driving both `setup.html` and `dashboard.html`'s dropdowns from the embedded manifests, with a client-side "custom" fallback. New `manifests/xai.json` (all pricing null/unconfirmed — first-party xAI pricing pages unreachable — `grok-code-fast-1` deliberately absent); `gemini.json` upgraded; `venice.json` verification-date refresh only; `"grok"→"xai"` alias. Fable's adversarial verify found zero blocking issues and four non-blocking ones, all fixed in-session (not deferred): a stale-async-render race in both HTML twins (fixed with a `renderGen`/`modalGen` token guard matching the files' existing `epoch` pattern), a tautological dead assertion in a new test (fixed), and a real UX gap where renaming a provider away from its preset key while leaving the model field blank broke server-side model auto-pick (fixed by falling back client-side to the preset's sample model — verified end-to-end via a live Playwright-driven browser session hitting the real running binary, confirming the exact POSTed request body). Zero edits to the two review-gated files | `gofmt`/`go build`/`go vet` clean, `go test ./... -count=1` all green (new `TestXaiManifest`/`TestGrokAliasResolvesToXai`/`TestPresets`/`TestProviderCatalogUnauthenticated`), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), live gateway smoke + Playwright against the real built binary (all 7 dropdown options, per-preset defaults, a real renamed-provider Add-provider round-trip, and the renamed-provider model-fallback fix all confirmed at the network-request level) | In review |
| Consent-gated full-protocol repo scaffold (branch + commit) | 2026-07-12 | Maintainer follow-up: registering/cloning a repo only ever auto-scaffolded a minimal `.l00prite/` memory subset silently — no `AGENTS.md`, no `CLAUDE.md` protocol section, no `.l00prite/prompts/`, no vendor adapters, nothing branched or committed — so a repo never got "the full benefit of the l00prite methodology." Built (ultracode off this pass — done directly, with targeted read-only Explore-agent research, not a Workflow pipeline): `gitx.Client` gained a read-only `CurrentBranch` primitive (both exec/gogit backends, one existing test double fixed to match); `engine.Files.ScaffoldFull` (new `internal/engine/protocol/` embedded mirror of `AGENTS.md.template`, the six canonical prompts, the vendor adapters, and the fixed CLAUDE.md protocol section — cli-os is a standalone portable binary with no runtime access to this monorepo's `templates/`, so it carries its own verbatim copy, same "separate copy kept in sync by hand" pattern `l00pfiles.go` already uses; never overwrites existing files) plus `FullProtocolGaps()` to skip a pointless empty branch when nothing's missing; new authenticated `POST /v1/repos/scaffold-branch` (creates a `l00prite/add-protocol-<random>` branch, scaffolds, commits, never pushes/PRs — returns copy-paste push instructions instead); dashboard gets a checked-by-default consent checkbox in the Register-repo modal plus a standalone "Add l00prite" button per repo card, both rendering through a shared result panel with a working copy-to-clipboard push command. A real bug was found and fixed by my own test (not a reviewer): the scaffold lock's release write was `defer`red, landing after the commit and leaving the new branch dirty — the same class of ordering bug this repo's PR #6/#7 rounds already fixed twice for the ledger append and the run engine's lease write; fixed by releasing explicitly before the commit. Zero edits to the two review-gated files (the new 8th prompt mirror is explicitly NOT added to the validator's byte-parity check — flagged as a queued, gated follow-up) | `go build/vet/test -race ./...` all green (new engine ScaffoldFull/FullProtocolGaps tests plus server-level tests against a REAL git repo via `os/exec`, asserting actual branch/commit/clean-tree state on disk), `gofmt` clean on every changed file, `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL, unchanged), live gateway smoke + Playwright driving both dashboard entry points against real local git repos, independently re-verified via `git branch --list`/`git log`/`git status --porcelain` on disk afterward | In review |
| PR #10 review round: Copilot + Codex findings | 2026-07-12 | PR #10 (this branch → `add-grok`) opened via the Claude Code UI and subscribed to for webhook activity. Copilot's review (3 findings, all fixed): a clipboard crash risk in dashboard.html when `navigator.clipboard` is unavailable (non-secure contexts, older WebViews) — added a `copyText()`/`legacyCopy()` execCommand fallback, verified live with `navigator.clipboard` stubbed to undefined; a repo-id trim-ordering cleanup in `repo_scaffold.go` (not an actual bug — `repoRootForToken` already trims internally — but tightened to match `HandleRepoRegister`'s pattern); a missing `git`-availability skip in the new server test, matching every other git-dependent test in this codebase. Codex's review (4 findings, all P2, each independently reproduced before fixing rather than trusted at face value): (1) a real gogit-only bug — acquiring the scaffold lock BEFORE the branch checkout dirties a tracked `lock.json`, and go-git's `Worktree.Checkout` (unlike real git's `checkout -B`) errors on any dirty tracked file even at the same commit, reproduced with a standalone test and fixed by reordering (peek the lock read-only, checkout, THEN acquire) — the same ordering class this repo's PR #6/#7 rounds already fixed twice, this handler just built it backwards; (2) `CommitUnit`'s `git add -A` silently drops scaffolded files a target repo's own `.gitignore` excludes — added `gitx.Client.AddPaths` (force-add bypassing `.gitignore`; go-git's single-path `Worktree.Add` already skips ignore-checking per its own source, no new abstraction needed) plus an exported `Files.FullProtocolPaths()` so force-adding covers files left over from an earlier partial scaffold too, not just what this call wrote (the first, narrower version of this fix was itself caught incomplete by its own regression test); (3) `FullProtocolGaps()` never checked the baseline memory files, so a partially-scaffolded repo could be misreported `already_complete` — fixed; (4) renaming a manifest-backed preset breaks bare/default-model routing (traced through `router.go`'s Rule 3/4) — root-caused to this branch's own earlier sample-model fallback, which removed the validation failure that used to guard against saving this exact broken state; a full fix needs a schema change (persist the preset key separately from the display name), judged architecturally significant rather than a small confident fix, so shipped a client-side warning in both HTML twins instead and flagged the deeper question to the maintainer. Gemini Code Assist's review had no actionable feedback (bot deprecation notice only) | `go build/vet/test -race ./...` all green (new `TestGogitCheckoutNewBranchFailsOnDirtyTrackedFile` pinning the reproduced go-git characteristic on both backends, `TestAddPathsBypassesGitignore`/`TestAddPathsEmptyIsNoop` on both backends, `TestFullProtocolGapsCatchesMissingBaselineFiles`, `TestRepoScaffoldBranchCommitsGitignoredProtocolFiles`, `TestRepoScaffoldBranchForeignLockRefusesWithoutCreatingABranch`), `gofmt` clean, `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), live gateway smoke + Playwright confirming the rename warning fires/clears correctly in both files | In review |
| Dark-theme dashboard fixes + OpenAI max_completion_tokens (v0.4.1-beta) | 2026-07-12 | Bug 1 (real, fixed): `.field.check input` (Add-provider panel's two checkboxes) relied on native `appearance:auto` plus `accent-color`, which only paints the checked state — added an explicit `background`/`border` so the unchecked box is never left to native dark-theme widget rendering. Bug 2 (investigated, not reproduced): live-rendered the "Models" modal (`editModels()`) across multiple providers/adapters (mock, openai-compat/Venice), viewport widths (1400px/390px), and disabled-model states via a locally-run gateway + Playwright against the real built binary — model-name labels were populated and correctly colored (`var(--text)`) in every case; traced the data path manifest→`ModelsFor`→`/v1/dashboard/summary`→`editModels()` end to end and found no empty-field cause either. Hardened `.modelrow span` with an explicit `color` anyway (previously relied on inheritance) as a no-risk precaution. Found and documented (not fixed — flagged as a state bug for a separate pass) a related real bug: `provCard()`'s "N models off" count reads `disabled_models.length` directly without intersecting the provider's live model catalog, so a stale `provider_models` row for a model no longer in the current manifest inflates the count on an otherwise-healthy provider card; reproduced by inserting a phantom disabled-model row and observing the card's count diverge from the Models modal's own (correct) list. Separately, mid-task, fixed a maintainer-reported OpenAI issue: the openai-compat adapter's `BuildRequest` — the single call point shared by both `gateway.TestProviderKey`'s pre-save validation ping and every real inference request — always sent `max_tokens`, which OpenAI's o-series and (per current web reports, not first-party-confirmed) gpt-5.x reject under `/chat/completions` in favor of `max_completion_tokens`; now renamed for model ids prefix-matching `o1`/`o3`/`o4`/`gpt-5`, scoped so other openai-compat upstreams (Zhipu, xAI, Venice, custom) are untouched. Updated stale `gpt-4o` placeholder text to `gpt-5` in both `dashboard.html` and `setup.html` — deliberately not a specific dated snapshot, since this repo's own `openai.json` manifest already documents that first-party OpenAI docs were unreachable from this environment and explicitly prohibits backfilling unconfirmed model ids from training memory; the same caution was applied to unverifiable claims in the request (e.g. an exact GPT-4o API retirement date, a "gpt-5.6" release) — not asserted anywhere in shipped code/docs. Zero edits to the two review-gated files | `go build/vet/test ./...` all green (new `TestOpenAICompatReasoningModelsUseMaxCompletionTokens` / `TestOpenAICompatNonReasoningModelsKeepMaxTokens`), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), live gateway (real built binary) + Playwright screenshots/computed-style checks for both dashboard bugs (checkbox background now `rgb(10,14,19)` vs. previously transparent; model-row text confirmed `rgb(231,237,245)` against the dark modal in every tested scenario) | In review |
| Auto-PR toggle, consent-gated push+PR, retry-tracking persistence, PR #13 (3 review rounds) | 2026-07-12 | PR #13: per-project Auto-PR toggle (`GatePush`/`GatePRCreate` only, off by default, explicit caller policy always wins) so an autonomous Run's push/PR-create can be pre-approved without per-action human waiting; the existing "Add l00prite" consent flow gained real push+`gh pr create` execution using ambient host git/gh credentials (never merges, always a human-reviewable PR). Three independent automated review rounds (gemini-code-assist, Copilot, chatgpt-codex-connector) each caught real gate-classification bypasses in `classifyCommand`/`classifyGitSub`, every one verified against the actual command text and fixed with a fail-first regression test: force-push flags with an attached `=value` slipping past exact-match; missing `shellChainChars` guard on `git push` (chainable via `sh -c`); `GatePush` covering any branch instead of only the run's own (including a refspec-embedded delete `origin :branch` / force `origin +HEAD:branch` that only the destination side was ever checked against); `gh pr create` flags that read an arbitrary local file into a public PR body (`-F`/`--body-file`, `-T`/`--template`, incl. pflag's attached-shorthand `-F/etc/passwd` form with no separator); `-R`/`--repo` targeting a repo other than the run's own; a missing `--head`; and — most severe — `--receive-pack`/`--exec`, real `git push` flags (confirmed via `git push -h`) that invoke an arbitrary program on the remote side of an SSH-transport push, the classic git-push-over-SSH RCE vector. Also built (Fable architect+advisor, Opus writer, per explicit request): a `repo_scaffold_attempts` gateway-side table so a capability-gapped "Add l00prite" push/PR (no `gh`, not signed in, bad remote) can be retried later instead of no-opping on `already_complete` — retry only recognized when the working copy is still on the exact tracked branch (the load-bearing guard against `EnsureRunBranch`'s checkout-B silently moving an abandoned branch), a pending row with no fresh `open_pr=true` stays read-only, a hand-opened PR is found via read-only `gh pr view` before any push. One real bug found by the advisor's own adversarial pass (a double `lookupExistingPR` call that could clear the pending row on a transient failure while reporting a null PR URL) and fixed. Zero edits to the two review-gated files or `internal/engine/`/`internal/gitx/` across all three rounds | `go build/vet/test -race ./...` green after every fix (each confirmed to fail against the pre-fix code first via temporary revert), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL, unchanged) | Merged (PR #13) |
| v0.5.0-beta release: version bump, changelog, APK rebuild, release-policy memory note | 2026-07-12 | Maintainer asked whether the website APK was current (it was not — still 0.4.0-beta, missing v0.4.1-beta's fixes and all of PR #13 above) and asked for a durable rule: rebuild the APK with every push, `+0.0.1` for minor changes, `+0.1.0` for major changes, never `1.0.0` before an explicit production release. Bumped `gateway.Version` to `0.5.0-beta` (a major bump: PR #13 shipped new capability, not just a fix), consolidated `CHANGELOG.md`'s stale "Unreleased" entry and the never-built v0.4.1-beta entry into a new v0.5.0-beta section describing the full Auto-PR/push+PR/retry/security-hardening arc, rebuilt+signed the APK via `cli-os/scripts/build-apk.sh 0.5.0-beta` (apt toolchain installed this session), replaced `downloads/l00prite-os-0.4.0-beta.apk` with the new build + regenerated `SHA256SUMS`, and updated every version/filename/SHA-256 reference in `index.html`. Added a new CLAUDE.md §9 "Android APK / Website Release Policy" so every future session rebuilds the APK on any `cli-os/`/`android/`-touching push and follows the same versioning scheme, rather than relying on being asked again | `apksigner verify` v2+v3 true, `aapt dump badging` confirms `versionName='0.5.0-beta'`, `sha256sum` matches the file actually placed in `downloads/`, `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL, unaffected) | In review |
| Connect-GitHub push + dark-mode WebView fix + bridge-mode coder fix (v0.6.0-beta) | 2026-07-12 | Two maintainer-reported bugs, designed by a Fable-authored design-of-record then implemented and adversarially reviewed. **Bug 1 (CSS black-on-black in the Android app):** the wrapper shipped no day/night theme and no WebView dark-mode control, so Android force-dark fought the page's own `@media (prefers-color-scheme)` theming. Fixed device-deterministically: new `android/res/values{,-night}/styles.xml` DeviceDefault day/night `AppTheme` + `AndroidManifest` `android:theme`; `MainActivity` disables force-dark (SDK-guarded `setForceDarkAllowed`/`setAlgorithmicDarkeningAllowed`/`setForceDark`) and appends `&theme=dark|light` from the device night-mode to the WebView URL; `dashboard.html`/`setup.html` gain a head script that stamps `data-theme` from `?theme=`/localStorage plus `:root[data-theme=dark|light]` override blocks (palette + body/topbar/btn + a theme-following modal/overlay scrim) that win the cascade regardless of what the WebView reports — desktop (no param) unchanged, and desktop light-mode modals, which had the same latent low-contrast bug, are fixed too. **Bug 2A (the run reported "no tool to git push"):** `PlanForObjective` armed `Bridge:true` for the default balanced/quality objectives, which routed coder turns through `RunBridge` — a path that advertises the repo tools but answers every non-bridge tool call with a capability-gap string and never executes it, so the coder could neither write files nor push. Fixed by forcing coder turns non-bridge (`exec.go` runCoder) + `Bridge:false` (reserved) in `roles.go`; planner/reviewer never bridged. **Bug 2B (the real feature — the AI can push, incl. on Android):** new "Connect GitHub" flow — `git_credentials` table (project,host-scoped, vault-sealed token, verified live via `GET /user` before storing, never returned by any endpoint, model-unreachable); `gitx.Client.Push`/`RemoteURL`/`ErrShallowPush` on BOTH backends (exec injects a host-scoped, APPENDED `GIT_CONFIG_*` extraheader — never argv/`.git/config`; gogit uses go-git `http.BasicAuth` + refuses shallow clones; a `credentialScope` guard binds the token to `github.com` so it can never reach a foreign/ssh/http origin, falling back to ambient instead); `ghapi.go` tiny REST client (verify/default-branch/create-PR/find-open-PR, bearer-only, token-redacted errors, no merge endpoint); `push_branch` engine tool (structurally pinned to origin + the run's own branch, non-force, GatePush-gated unless Auto-PR, honest capability-gap on gogit+no-cred); `scaffold_pr.go` gains a token-first push/PR path (works with no `git`/`gh`) with nil-auth byte-identical to before; gogit clones full-depth so they can push; dashboard "Connect GitHub" card + modal. Fable designed the record and the security controls; Opus implemented, ran the 5-lens adversarial review workflow (+ per-finding verify), and did the live verification. Review caught/fixed: a `push_branch` token-leak-to-foreign-origin (closed at the gitx seam via `credentialScope`), an ssh-origin scaffold regression (same fix), a misleading token-path push-fail message, and two test-adequacy gaps (a real `Push`-level token-not-in-argv/`.git/config`/redaction test, and `openScaffoldPR` token-path coverage). Live Playwright verification against the real built binary caught a real dark-scrim-twin bug (fixed) before it shipped. Rebuilt+signed the APK as `l00prite-os-0.6.0-beta.apk`; `CHANGELOG.md`, `index.html` (version/filename/SHA-256), `downloads/` updated to match. Zero edits to the two review-gated files | `go build/vet/test -race ./...` all green (new gitx push/credential-scope/token-redaction/shallow tests on both backends, `github.go`/`ghapi.go` connect-flow + scaffold token-path tests, engine `push_branch` gate/capability/approved-push tests, coder-non-bridge regression), `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), live gateway + Playwright (26 checks: `?theme=` overrides the opposite `prefers-color-scheme` in all four scheme combos, modals readable in both themes, GitHub card/connect-modal render, desktop unchanged) + agent-viewed dark/light modal screenshots, `apksigner verify` v2+v3 true, `aapt dump badging` `versionName='0.6.0-beta'`, `sha256sum -c` against the file placed in `downloads/` | In review |
| PR #16 review rounds (Gemini + Copilot + Codex ×2) | 2026-07-13 | PR #16 (this branch → `main`) opened from the v0.6.0-beta Connect-GitHub/dark-mode branch above and subscribed to for webhook activity; three automated reviewers left 11 threaded findings across two Codex passes, every one addressed and its thread resolved. **Engine push path (`internal/engine/tools.go`):** Codex P1 — the approved push ran before the unit commit, so origin received the pre-unit HEAD while the verified change committed only locally; fixed by deferring — `push_branch` records intent (`PushRequested`, returns `push_scheduled`) and the engine performs the real push in `PerformPendingPush` AFTER `CommitUnit` (`engine.go:490`). Codex round-2 P2 — a gogit/no-git host would schedule a push for ANY stored token even when origin is an ssh/http/non-github remote the token can't attach to (failing unauthenticated after commit); now checks `gitx.TokenUsableFor` up front and returns an honest capability gap. Codex round-2 P2 — the deferred push widened the approval→push window, so edits made after approval would be pushed; `Execute` now freezes mutating tools (`write_file`/`run_command`/`git_command`) once a push is scheduled, so the pushed state == the approved state (read-only tools stay; repeat `push_branch` idempotent). **Scaffold push/PR path (`internal/gateway/scaffold_pr.go`):** Copilot + two Codex P2s — a connected github.com token broke the previously-working ambient git/gh Add-l00prite path for ssh/non-github origins (one path took the token route, another hard-failed); fixed uniformly at the caller (`repo_scaffold.go:107` nils `scaffoldAuth` via `tokenUsableForOrigin` whenever origin isn't an https github.com remote the token can serve, so probe/push/PR-lookup all fall back to ambient together). **REST (`ghapi.go`):** Copilot — `ghDo` now sends `User-Agent: l00prite-os/<version>` (GitHub 403s without one); Gemini — `ownerRepoFromURL` uses `u.Hostname()` for port-robust host matching. **git seam (`internal/gitx/`):** Gemini — the push env strips a pre-existing `GIT_CONFIG_COUNT` before appending so git's first-match `getenv` reads the injected count; Codex — `RemoteURL` uses `git remote get-url --push` so credential-scoping/owner-repo resolution follow the push URL; Gemini — `credentialScope` uses `u.Hostname()`. Every fix carries a regression test confirmed to fail against the pre-fix code (temporary-revert verified). The v0.6.0-beta APK was rebuilt to embed the round-2 engine changes (`downloads/` + site SHA-256 refreshed). Zero edits to the two review-gated files | `go build/vet/test -race ./...` all green, `node scripts/validate-l00prite.js` (519 PASS, 0 FAIL), the two round-2 regression tests confirmed failing against reverted `tools.go` then passing, `apksigner verify` v2+v3 true + `aapt dump badging` `versionName='0.6.0-beta'` on the rebuilt APK, both PR #16 CI checks (Vercel + real-Android-SDK APK build) green | In review |

## 8. Completion Criteria

l00prite v1.1 (this branch) is complete when:

- [x] All six protocol layers exist and pass the validator with zero FAIL lines.
- [x] Prompt parity is byte-exact across Claude, Codex, canonical, dogfood, and example
      copies — enforced mechanically, not by convention.
- [x] Every major AI agent has a discovery path into the protocol (AGENTS.md ecosystem,
      CLAUDE.md protocol section, GEMINI.md/QWEN.md, copilot-instructions, Cursor/Windsurf
      rules, CONVENTIONS.md), each self-sufficient.
- [x] Execution Mode exists end-to-end: disarmed-by-default schema, pre-flight gate,
      iteration protocol, nine run boundaries, resumable exits, `--execute` handoff.
- [x] README, AGENTS.md, and HANDOFF.md accurately describe current behavior, with no
      claimed capability the repo doesn't actually have.
- [ ] The maintainer has reviewed and merged this branch to `main` (covers the two
      review-gated files: `.claude/commands/build-loop.md`, `scripts/validate-l00prite.js`).

What's next, tracked in `.l00prite/todos.md`:

- [ ] **v1.2 gated batch** (needs review of the two review-gated files, so held out of the
      2026-07-04 ungated pass): formal `no_progress_detected` + wall-clock-first
      `budget_exceeded` boundaries (nine → eleven), a machine-parseable run-log, phased
      autonomy levels (restriction ladder), an independent verifier prompt, and a pattern
      library. Enumerated as one coherent batch in `.l00prite/todos.md`.
- [ ] Runtime harness that mechanically enforces run boundaries and iteration budgets.
- [ ] GitHub event ingestion and CI failure capture as events.
- [ ] Cross-agent compatibility tests, including a mid-execution boundary stop and resume
      by a different vendor.

## 9. Android APK / Website Release Policy

The marketing site (`index.html`) and the `downloads/` folder are this repo's own distribution
channel for the L00prite OS Android app — the shipped APK must never trail merged source.

- **Every push to `main` that touches `cli-os/` (gateway, dashboard, adapters) or `android/`
  must rebuild the APK** via `cli-os/scripts/build-apk.sh <version>` (hermetic toolchain: apt
  `aapt`/`zipalign`/`apksigner`/`dalvik-exchange`/`android-framework-res`/`openjdk-21-jdk-headless`
  (or `default-jdk`, for `javac`/`keytool`), no Google-hosted downloads — install via apt if
  missing), copy the signed output into `downloads/`, regenerate
  `downloads/SHA256SUMS`, remove the old version's `.apk` from `downloads/`, and update every
  version/filename/size/SHA-256 reference in `index.html` (nav CTA, hero download button, and
  the Android section's download button + `#sha-val`). Verify with `apksigner verify` (v2+v3
  true) and `aapt dump badging` (confirms `versionName`) before shipping — never claim a build
  succeeded without checking its actual output.
- **Versioning is `MAJOR.MINOR.PATCH-beta`.** Bump `PATCH` (+0.0.1) for a minor change (a bug
  fix, a small UI tweak, docs). Bump `MINOR` (+0.1.0, reset `PATCH` to 0) for a major change
  (a new feature, a security fix, a schema/API change, anything a user would notice as new
  capability rather than a polish pass). **Never reach `1.0.0` until an explicit
  production-release decision is made** — betas stay `0.x.y-beta` indefinitely until then.
- `internal/gateway/dashboard.go`'s `Version` var, the APK's `--version-name` (set from the
  same string passed to `build-apk.sh`), `CHANGELOG.md`'s newest heading, and `index.html`'s
  displayed filename/SHA-256 must all move together as one atomic set — never independently,
  never left to drift.
- If a source-only pass genuinely cannot build (no Android toolchain available in that
  session), it must say so explicitly in `CHANGELOG.md` (see the "source only" precedent
  already in this file) rather than silently skip the build — but the very next session that
  *can* build must fold that gap in before shipping anything new, not leave the site
  permanently behind source.

<?xml version="1.0" encoding="utf-8"?>
<!--
  L00prite OS Android wrapper manifest.

  versionCode / versionName are deliberately NOT declared here: both build pipelines
  (cli-os/scripts/build-apk.sh and .github/workflows/android-apk.yml) inject them at
  package time via the "version code" and "version name" flags of aapt package / aapt2
  link, keeping one source of truth (the release VERSION string) instead of duplicating
  it into this file.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.l00prite.os">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required for ConnectivityManager#getActiveNetwork / #getLinkProperties, used to
         resolve the device's real per-network DNS servers (LOOPRITE_DNS, gap G1 in
         android-architecture.md). Normal-protection permission: granted automatically
         at install, no runtime prompt. Not in the brief's literal permission list but
         required for the DNS-resolution behavior the brief specifies; see android/README.md. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="@string/app_name"
        android:extractNativeLibs="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".GatewayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
